server:
  host: "0.0.0.0"
  port: 8000

browser:
  headless: false
  timeout_ms: 10000

router:
  default_platform: "gemini"
  role_registry:
    gpt: "chatgpt"
    gemini: "gemini"
    claude: "claude"
    deepseek: "deepseek"
